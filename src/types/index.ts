export interface Service {
  icon: string;
  title: string;
  description: string;
}

export interface Testimonial {
  name: string;
  stars: number;
  text: string;
  role: string;
}

export interface NavLink {
  label: string;
  id: string;
}
